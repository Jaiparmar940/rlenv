# CLAUDE.md — no-start-env

## What this is

An agentic RL environment + eval: a simulated vehicle with electrical no-start/charging faults that an LLM agent diagnoses using realistic tools, scored by a cheat-resistant grader. Built on **Inspect (inspect-ai)** so labs can run it directly. This repo is the founder's first public artifact for a company selling physical-systems environments/evals to AI labs and robotics companies — it publishes **Monday**, with a benchmark writeup showing where frontier models fail physical diagnosis. Quality bar: a lab eval engineer reads this code cold and takes the author seriously.

## Division of labor (important)

- **Cursor** executed the initial phased build (Phase 1 skeleton). Check `git status`/`git log` before touching anything — do not duplicate or clobber in-flight work from another tool. If a file was just modified, ask before rewriting it.
- **The human (Jaivir) is the domain verifier.** He has caught four physics bugs that passed the automated test suite (load-sag inversion; downstream node reading above upstream; red-herring battery drifting into genuinely-weak territory; ground-fault drop too small to characterize). When physics is uncertain, FLAG IT — never guess plausible-looking numbers. Plausible-but-wrong is the failure mode this whole product exists to prevent.
- **Claude Code (you):** implementation, tests, refactors, transcript analysis, writeup drafting. Stop at phase checkpoints for human sign-off. **Keep this file's "Current state" section up to date as work lands, and commit it with the change it describes.**

## Dev environment (this machine)

- Run everything via `.venv/bin/python` (uv-managed CPython 3.12; system python3 is 3.9 and unusable). `uv pip install -e ".[dev]"` after a fresh clone.
- Provider keys live in gitignored `.env` (template: `.env.example`); `scripts/run_evals.py` loads it.
- Mobile play UI: `scripts/webapp.py` (serves :8642; expose with `cloudflared tunnel --url http://localhost:8642`). Committed 2026-07-11 by Jaivir's call as a dev/verification tool — may be removed before/after publish.

## Architecture

- **Resistance-network model:** the circuit is nodes connected by resistances with a battery source. Faults = bump ONE resistance (or battery internal params). Engine states = current draws (key_off ≈ 0 A, key_on small, cranking ~150–200 A). All voltages COMPUTE via V = I×R. **Never hand-author output voltages** — change R or I so correct readings emerge. ~15 physical constants generate everything; they live in domain/ and are documented in DOMAIN_TRUTH.md.
- **Nodes:** battery_positive, battery_negative (reference, 0 V), engine_block, starter_stud, alt_output, chassis.
- **Two-point measurement:** `measure_voltage(point_a, point_b, engine_state)` returns V(a)−V(b) + seeded noise (±0.05 V). Invalid node or state RAISES. Sign flips when args swap.
- **Tools:** scan_dtcs, read_pid, measure_voltage, visual_inspect, replace_part, attempt_start, finish(diagnosis). Tool outputs are instrument readings ONLY — no explanations, and NOTHING that leaks ground truth (fault names, component health).
- **Ground truth** lives in `src/nostart/domain/` and `World._active_faults`; never serialized into observations, prompts, or tool outputs.
- **Determinism:** every scenario seeded; identical runs → identical observations and scores.

## Physics invariants (encode the four caught bugs — never violate)

1. Cranking voltage < resting voltage at every point (load sag, never inverted).
2. No downstream node exceeds an upstream node at rest (resting monotonicity down the supply path).
3. Red herrings are RESTING-ONLY overrides and suppress the whole resting supply path uniformly.
4. In ground-fault scenarios the fault must be uniquely localizable: large drop across the ground path (battery_negative→engine_block) under cranking ONLY, small drop across the positive feed (battery_positive→starter_stud), battery itself holds (innocent).
5. A red-herring battery must read tempting at rest (~11.8 V) but PASS a load test under cranking (hold > 9.6 V; currently ~10.5 V from the series-derived recovery) — bait, not a co-fault.
6. Ground-current recovery is DERIVED, never tuned: recovery/drop = healthy_sag/OCV = 2.8/12.6 ≈ 0.222 (series-consistency ruled by Jaivir 2026-07-12). Any change to the nominal battery constants moves it automatically.

## Scenarios

Cranking battery numbers below reflect the 2026-07-12 series-consistency recalibration (play-verification of the two ground scenarios predates it; ground drops and feeds are unchanged).

- `easy_dead_battery` — battery ≈ 2.1 V all states, no_click, ground drop ≈ 0. VERIFIED by play.
- `medium_corroded_ground` (1.1 Ω strap) — good battery; cranking: battery ~10.41 V (passes load test), ground drop ~2.75 V, feed ~0.2 V; slow_crank.
- `medium_ground_red_herring_battery` (1.2 Ω strap) — resting path suppressed to ~11.8 V (bait, cleared if battery replaced); cranking: battery holds ~10.47 V (passes load test), ground drop ~3.0 V is the tell.
- `hard_intermittent_ecu_can` (seed 3035, manifest 0.35 deterministic per seed) — all voltages nominal; crank_no_start on ~35% of cranks, U0100/degraded CAN intermittently. Signed off 2026-07-12.
- `hard_compound_battery_and_ground` — PRIMARY ground_strap:corroded 0.7 Ω (drop ~1.78 V crank), SECONDARY genuine battery:weak terminal_drop 1.6 (11.0 V rest, ~8.59 V crank — fails load test). Neither repair alone starts the car. Root-cause 60 splits 45/15. Signed off 2026-07-12.

## Current state (updated 2026-07-12)

**DONE:**

1. **Phase 1 — environment + scenarios.** Ground-drop magnitude fix applied to spec (scenario resistances 1.1/1.2 Ω, GROUND_CURRENT_RECOVERY 0.6); red herring clears when its component (battery) is replaced. DOMAIN_TRUTH.md signed off by Jaivir 2026-07-10.
2. **`scripts/sanity_check.py` green**, thresholds pinned to the spec targets (battery ≥ 11.3 — moved to ≥ 10.3 by item 9's recalibration, drop ≥ 2.5, feed ≤ 0.5) so a too-soft fix cannot pass.
3. **Phase 2 — grader.py + tests:** score 0–100 (root cause 60 / parts discipline 25, −8 per wrong part / cost efficiency 15 vs expert baseline, $2 ≈ 1 min). Wrong-part penalty ALSO debits the total so a measure-once parts-cannon can't ride root-cause points past 50. Anti-cheat: symptom masking ≠ success; finish() with no measurements caps at 40. Four adversarial agents defeated. CHECKPOINT DONE 2026-07-10; adversarial scores after the 2026-07-11 grading changes: 0 / 0 / 40 / 9, all < 50; expert 100.
4. **Full-resolution grading (Jaivir's call, 2026-07-11).** All scenarios are fix-the-car, not diagnose-only: expert baselines include repair + verify (easy 25 min/$180, ground 34 min/$25, herring 37 min/$25 — each derived from an explicit expert action sequence against costs.py, documented inline in scenarios.py). Grader applies a flat −15 RESOLUTION_PENALTY unless the root-cause part was replaced AND attempt_start returned "starts" AFTER the last replacement — correct diagnose-only finishes and correct-but-unverified repairs both take it; wrong-part symptom relief never verifies (root-component check). Task system prompt states the requirement. Resolves the former expert-baseline-semantics open decision. NOTE: existing eval means (item 6) predate this and need a rerun.
5. **Efficiency + guessing-cap hardening (Jaivir's call, 2026-07-11, after webapp play exposed both).** (a) Blind part-swap scored 100: the required verify crank counted as the probe that dodges the guessing cap. Cap now keys on `diagnostic_probe_count` — probes BEFORE the first replacement; blind swap → 40. (b) 19-action flail run scored 96.5: parts dollars in the cost ratio diluted a 2.4× time overrun to 1.3×, and the 15-pt bucket floors at 0. Cost efficiency is now TIME-ONLY (parts $ already policed by parts discipline), linear from 15 at ≤1× expert time to 0 at 2×, going NEGATIVE beyond (debits the total, mirroring the wrong-part rule; total floored at 0). Flail run → 79. Known consequence, accepted: crisp diagnose-only (85) outscores sloppy full-resolution (79). Known gap: wrong-part penalty is a flat −8 regardless of part price ($15 fusible link = $450 ECU) — parts dollars now influence no bucket; price-weighted penalty is a v0.2 candidate.
6. **Phase 3 — src/nostart/task.py (Inspect) + scripts/run_evals.py built and run** (3 models × 3 scenarios × 3 epochs, 2026-07-10). Real prose broke the diagnosis parser twice (length-based component match; enum-order mode match hijacked by red-herring exoneration wording) — both fixed with positional + component-valid-mode matching, regression-tested against the actual model answers. 2026-07-12 rerun under full-resolution grading, grok/grok-4 added (4 models × 3 × 3): fable-5 98.2 / sonnet-5 82.4 / grok-4 78.9 / gpt-5.5 71.0 — but Jaivir's transcript review showed the sub-fable gaps were mostly grader misreads, not model failures (item 7).
7. **Parser fixes round 3 (2026-07-12, from the rerun transcripts).** (a) Compound hijack: "engine-to-battery ground strap" parsed as battery via first-mention — a fully correct sonnet red-herring answer scored 0/60 (34.32 total). Component mentions inside node names ("battery negative") or X-to-Y hyphen compounds are now shielded. (b) Mode vocabulary (open decision (d), now resolved): correct answers phrased "high resistance" (strap) or "internally failed / severely discharged / shorted-sulfated cell" (battery) got mode=null, −30 each — this accounted for most of the model ranking. `_MODE_SYNONYMS` maps tech phrasing to candidate modes resolved against the component's valid modes ("high_resistance" → corroded for strap, literal for fusible_link); entries added ONLY when justified by a real transcript. All six answers pinned as regression tests. `results/` NOT regenerated — the 2026-07-12 table still reflects the old parser; re-grade or rerun is Jaivir's call. Seventh entry added 2026-07-12 from the hard-tier haiku run (Jaivir's call): "low capacity" → weak (battery) — a verified two-part compound fix phrased "aged battery with low capacity" took component-only secondary credit (88.3, would now be 95.8); bare "failed" deliberately NOT mapped (doesn't disambiguate weak vs dead). Pinned verbatim in test_hard_tier.py.

**BRANCH `eval/uncoached-prompt-reporting` (merged to main 2026-07-11):**

- **Uncoached prompt is now the default** (src/nostart/prompts.py): role + tool reference + JOB definition + cost one-liner + finish() format ONLY. The old prompt (which dictated measure-first / isolate-repair-verify and enumerated grader rules) is preserved verbatim as `--prompt-variant coached`, recorded in every results row and transcript header.
- **Fairness fix (branch `fix/uncoached-repair-requirement`, Jaivir's call after a fable 85.0 transcript):** the grader demands repair + verified start, but the uncoached prompt/user message only said "diagnose" — a perfect diagnose-only episode lost 15 pts to an unstated requirement. Prompt and user message now state the JOB ("find the fault and fix the vehicle", "scored on ... the vehicle actually being fixed (confirmed by a successful start)") with still no strategy or penalty enumeration. Mock expert scripts updated to full resolution (repair + verify → 100 again). Any uncoached results recorded before this fix under-score diagnose-only models.
- **Reporting upgraded:** per model × scenario — mean/min, subscore means, pass^k (all epochs full root credit), root-ok / verified-fix / measured-first rates; summary adds best-worst model spread. Per-episode detail kept as appendix.
- **`scripts/smoke_eval.sh`:** one mid-tier model (default haiku-4-5) × 3 scenarios × 3 epochs, uncoached — the cheap post-change gate.
- **Smoke A/B (superseded — see item 8): haiku-4-5 coached 57.4 vs uncoached 38.3.** Run under the pre-`running`-state prompt; re-measured in item 8.

8. **v0.1 CLOSEOUT (2026-07-12/13, agent-executed).**
   - **Published matrix RERUN under one prompt.** The prior table was invalid as a comparison: its 36 frontier episodes ran under the *previous* uncoached prompt (pre-`read_pid` "current state" paragraph, commit 76b88f6) while haiku's 9 ran under the current one. Caught by the leak/integrity audit. Full matrix rerun 5 models × 3 scenarios × 3 epochs uncoached, single prompt revision: **grok-4 97.9 / fable-5 96.2 / gpt-5.5 93.0 / sonnet-5 91.4 / haiku-4-5 47.9**; frontier 36/36 full root-cause credit, separation 50.0. The prompt fix moved things only slightly (frontier 93.7→94.6, haiku 39.8→47.9) — the finding survived it.
   - **Deployment-tier failure mode re-characterized from the new transcripts (the old "condemns the alternator 4/9" line was stale).** haiku's real failure on the ground scenarios is a **parts cannon**: across the six ground episodes it replaced 11 innocent parts (battery 5×, alternator 4×, starter_motor 2×) — including in the episode where it inspected the strap, said *"Aha! The ground strap end is greenish"*, and still scored 11.7 because it had already swapped two innocent parts and never verified a fix. Red-herring scenario: **0/3 verified fixes**. Note for grader design: `measured-first` is 3/3 for these episodes — it always measures once, so the guessing cap never fires. Only the wrong-parts-debit-the-total rule catches measure-once-then-shotgun.
   - **Coached A/B re-measured under the current prompt pair:** coached 61.5 vs uncoached 47.9 = **13.6-point coaching effect**; verified-fix 8/9 → 5/9. Coaching does not rescue it (red herring still 20.9 coached).
   - **Audit (results/audit.md): 4/4 PASS** — zero ground-truth leakage across all 45 transcripts (only designed content: complaint bait, battery visual note, finish() format example); uncoached prompt confirmed strategy-free and rule-free; sanity_check exit 0 + 70/70 tests; determinism verified by new `scripts/check_determinism.py` (identical observation streams across builds AND processes).
   - **Clean-clone verified (results/clean_clone.md).** Found a real publish blocker: a cold clone **could not run any real model** — Inspect ships provider SDKs as optional and none were declared, so `--models anthropic/...` died with a PrerequisiteError. Fixed: `pyproject.toml` now declares `python-dotenv` (was only arriving transitively) and a `[models]` extra (anthropic/openai/google-genai/xai-sdk). Install line is now `pip install -e ".[dev,models]"`.
   - **New:** `scripts/scale_curve.py` (standing results format → results/scale_curve.md), `scripts/check_determinism.py`, `scripts/README.md` (eval surface vs dev tools), WRITEUP.md, README.md (rewritten), V0.2.md, YC_APP_DRAFT.md, LICENSE (MIT), CITATION.cff, CLOSEOUT_STATUS.md.
   - **.gitignore now publishes the reports:** `results/*.md` tracked (results.md, scale_curve.md, audit.md, clean_clone.md); `results/logs/` and `results/transcripts/` ignored.
   - **NOT DONE — open 3B-8B tier.** No OpenRouter key. Config wired (`.env.example`, model ids pinned in scale_curve.py); rows read `NEEDS_API_KEY`. This is the one blocking item for the scale-curve story.
   - **Hard tier built on branch `hard-tier-preview` (commit 1bcac7d)** — MERGED to main 2026-07-12 after Jaivir cleared all 7 sign-off boxes (see item 9). Scenarios `hard_intermittent_ecu_can` (0.35 manifest, deterministic per seed) + `hard_compound_battery_and_ground`. Compound rule: root-cause budget SPLITS (primary 45 / secondary 15) so compound episodes stay on the same 0-100 scale; secondary replacement is not a wrong part. Smoke (haiku, 6 eps, pre-recalibration): compound 68.5, intermittent **0.0** (never suspects the ECU; rides a lucky crank to a false "fixed") — accepted by Jaivir as correctly hard.
   - ~~**PHYSICS FLAG ON MAIN**~~ (`GROUND_CURRENT_RECOVERY` vs series solve) — RESOLVED 2026-07-12, see item 9.

9. **Series-consistency recalibration + hard-tier merge (Jaivir's ruling, 2026-07-12).** Jaivir cleared every box in PENDING_HUMAN_PHYSICS_SIGNOFF.md (11.0 V/9.2 V compound battery believable; 1.78 V drop characterizable; 0.7 Ω strap ratified; crank_no_start @ 0.35 manifestation ratified; 45/15 split + mention-based compound parsing ratified; intermittent-is-brutal accepted) and RULED on §4: **the model must close against the series solve, in both tiers.** `GROUND_CURRENT_RECOVERY` is now DERIVED in propagation.py — recovery/drop = healthy_sag/OCV = 2.8/12.6 ≈ 0.222 (exact for any strap R in a linear series circuit; was hand-tuned 0.6). New calibration: corroded-ground battery cranks ~10.41 V (was ~11.45), herring ~10.47 V (was ~11.6), compound weak battery ~8.59 V (was ~9.22; resting 11.0 V unchanged). Ground drops, feeds, and resting readings all unchanged. Every tell survives: innocents still pass the 9.6 V load test, compound battery still fails it. `BATTERY_HOLDS` floor 11.3 → 10.3 in sanity_check.py; invariant 5 restated as "passes a load test"; new invariant 6. 104 tests + sanity_check + check_determinism green. **CONSEQUENCE: the published results table (results/results.md, 45 episodes) was run under the OLD calibration — models saw ~11.45/11.6 V readings that the environment no longer produces. Table needs a rerun before publish; results.md carries a staleness notice.**

10. **Final k=5 published table (2026-07-13, commits `1bc12ca`..`a41017c`, pushed).** Gemini + OpenRouter keys landed; matrix expanded to **9 models × 5 scenarios × 5 epochs = 225 episodes** (added google/gemini-3.5-flash — 2.5-flash is use-blocked for new keys — and the open 3B-8B tier; the 3B id moved twice: qwen-2.5-3b delisted from OpenRouter, llama-3.2-3b lacks tool-capable endpoints, pinned `mistralai/ministral-3b-2512`). Epochs 4-5 merged onto the k=3 corpus with relabeling; llama-8b's +2 pass was replaced by a clean retry after transient CancelledErrors. **Results: fable 86.0 / gpt 82.3 / grok 74.9 / sonnet 74.5 / gemini-flash 59.7 / haiku 39.9 / ministral-3b 23.0 / qwen-7b 20.9 / llama-8b 7.2; separation 78.8. Frontier 59/60 root-ok on easy/medium, 15/40 on hard; NO model passes^k the full set.** Notables: sonnet owns compound (4/5 both-fault) but 0/5 on intermittent; grok never reports the compound secondary; haiku red herring 0/5 with 14 innocent parts; ministral-3b 5/5 on easy (tool mechanics fine, localization absent); 50-message cap kills some correct intermittent episodes mid-verify (kept deliberately, documented in WRITEUP limitations). Audit re-passed 4/4 over all 225 transcripts. scale_curve.py findings now computed from the corpus. README/WRITEUP/YC/CLOSEOUT numbers updated to this table (uncommitted, pending Jaivir's voice edit). Coached-A/B numbers in WRITEUP are marked directional (measured pre-recalibration).

**OPEN — decisions on Jaivir (block the final published table):**

- **Scores look inflated; causes identified:** (a) ~~system prompt topology hint~~ — RESOLVED (merged from `calibrate/neutral-prompt-grok`, 2026-07-11): hint removed, rerun gave sonnet 96.7 / fable 88.7 / gpt 75.2 (−2.9/−5.4/−1.8) — models infer topology by probing, so the hint was worth little. NOTE: those means (and item 6's) predate full-resolution grading — final table needs a fresh run; (b) rubric floor is high (component-only 30 + clean-hands 25 + terse 15 = 70) — partially addressed by the −15 resolution penalty; (c) visual_inspect reveals strap corrosion 40% of the time — electrical localization should be the only reliable tell?; (d) ~~mode vocabulary~~ — RESOLVED 2026-07-12 by `_MODE_SYNONYMS` (item 7).
- ~~**Framed-alternator trap**~~ (fable easy 26.6 neutral run; recurred as 32.2 uncoached) — RESOLVED 2026-07-12, pulled forward from v0.2 on Jaivir's call: `running` engine state added with charging physics. World tracks engine-running (set by attempt_start→starts; cleared by replace_part or measuring any non-running state); measure_voltage("running") raises unless running; read_pid reflects the live state (healthy alt ≈ 14.4 V, idle ≈ 700 rpm after a successful start). Alternator faults have distinct running signatures (no_output: rail at battery ~12.4, NO rise, post ≈ battery not 0 V; diode_failure: ~13.2 weak charge). Resting monotonicity + red herrings stay resting-only by construction. New `check_running_charging` in sanity_check.py; running-state charging physics (14.3/14.4 charging, 12.4 no-output rail, −1.2 diode delta, 700 idle) **SIGNED OFF by Jaivir 2026-07-14** — DOMAIN_TRUTH.md sign-off checklist fully cleared.
- **Grok support wired:** Inspect's native provider, `grok/<model>` ids, XAI_API_KEY in .env.example — needs a key in .env to run.
- **Phase 3 CHECKPOINT:** Jaivir reads `results/transcripts/`, tags failure modes. Banked so far from human play: supply-referenced readings hide ground faults; wrong ground reference frames an innocent starter.

**THEN — publish prep:** README (quickstart, results table, failure-mode taxonomy, honest known-gaps: feed drop not current-scaled, single-vehicle ground topology assumption, mode-vocabulary strictness; v0.2 notes: harder scenario set — intermittents, co-faults, more scenarios), human baseline row, repo hygiene. Publish Monday 2026-07-13.

11. **Publish-prep pass (2026-07-14, agent-executed).** README.md rewritten from hard-wrapped prose to standard soft-wrapped paragraphs (content/numbers unchanged; grader bucket list and results narrative restructured for scan-ability). Physics fully signed off: DOMAIN_TRUTH.md running-state checklist box cleared (2026-07-14), status header updated, stale "PENDING SIGN-OFF / do not publish" banner removed from scenarios.py hard tier. **92 `TODO(VERIFY)` draft markers swept from `src/`** (pure comment removal — explanations kept, bare markers dropped; 105 tests + sanity + determinism re-verified green, zero behavior change). **Fixed a clean-clone publish blocker:** `run_evals.py --mock` KeyError'd on both hard-tier scenarios (MOCK_SCRIPTS never extended when the hard tier merged) — added expert mock scripts for `hard_intermittent_ecu_can` and `hard_compound_battery_and_ground` (mirroring the documented expert paths + test-pinned finish phrasings); all 5 scenarios now mock-score 100.0. `webapp.py` confirmed shipping as a dev tool (Jaivir's call). KNOWN PAPERCUT (not fixed): `--mock` overwrites the tracked `results/results.md`; restore with `git checkout` after running, or redirect in v0.2.

12. **Company landing site moved OUT of this repo (2026-07-17, Jaivir's call).** A Scale.com-style one-pager was drafted here on branch `site/landing-page`, then migrated to its own public repo **https://github.com/Jaiparmar940/groundtruth-site** (local: `~/Desktop/groundtruth-site`), hosted on GitHub Pages at https://jaiparmar940.github.io/groundtruth-site/. The branch was deleted after migration; this repo stays website-free. The site's `CLAUDE.md` documents which rlenv results it cites and which of those figures are stale (its table shows the 2026-07-12 k=3 run, not the final k=5 table) — sync it from `results/results.md` before any real launch.

## Definition of done (v0.1)

`pip install -e . && python scripts/sanity_check.py && python scripts/run_evals.py --scenarios all` → all checks pass, scored table generates, tests green on a clean clone. Public Monday.

## Do not

- Hand-author voltages; leak ground truth into any observation; break determinism.
- Add RL training code, Docker, or CI in v0.1. (Web-UI ban lifted 2026-07-11: `scripts/webapp.py` is committed as a dev/play tool; candidate for removal later.)
- Mark any phase done without its human checkpoint.
- Silently "fix" physics you're unsure of — surface it as a question with your best guess and the reason for doubt.
